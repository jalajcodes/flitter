<%= provide(:title, "Sign Up")%>
<div class="min-h-screen pt-16 bg-gray-50 flex flex-col justify-center py-12 sm:px-6 lg:px-8">
  <div class="sm:mx-auto sm:w-full sm:max-w-md">
    <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">Create a new account</h2>
  </div>
  <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
    <div class="bg-white pt-2 pb-8 px-4 shadow sm:rounded-lg sm:px-10">
      <%= render "form" %>
      <div class="mt-6 flex items-center justify-center">
        <p>
          Already have an account? 
          <%= link_to "Login now!", login_path, class: "font-medium text-indigo-600 hover:text-indigo-500" %>
        </p>
      </div>
    </div>
  </div>
</div>
