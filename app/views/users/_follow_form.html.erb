<% unless current_user?(@user) %>
  <div id="follow_form" class="grid place-items-center mt-5">
    <% if current_user.following?(@user) %>
      <%= render 'unfollow' %>
    <% else %>
      <%= render 'follow' %>
    <% end %>
  </div>
<% end %>
